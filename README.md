# Calculadora-em-Python---EBAC
Projeto de estudo, criação de uma calculadora simples, com Python.

Este repositório contém:
- `calculadora.sh`: calculadora em Bash (interativa via terminal)
- `calculadora.py`: calculadora equivalente em Python
- `comandos.txt`: comandos usados para permissões/execução (referência)

---

## Como executar o arquivo `.sh` (Bash)

### Pré-requisitos
- Linux ou macOS
- `bash`
- `bc` (usado para cálculos no script)

### Passos
1) Entre na pasta do projeto:
```bash
cd primeiro-projet
